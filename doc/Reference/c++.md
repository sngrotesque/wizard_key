规则 of five/three/zero
规则 of zero：如果类不需要自定义析构函数、复制构造函数、复制赋值运算符、移动构造函数或移动赋值运算符，那么最好使用默认实现（例如，通过智能指针管理资源）。
规则 of three：如果类需要自定义其中一个复制操作（复制构造函数或复制赋值运算符），那么通常也需要自定义另一个复制操作以及析构函数。
规则 of five：在C++11及以后，如果类需要自定义其中一个复制操作或一个移动操作，那么通常也需要自定义所有五个（析构函数、复制构造函数、复制赋值运算符、移动构造函数、移动赋值运算符）。